<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#2563eb">
    <title>Planning Agents 2026</title>
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22 fill=%22%232563eb%22>📅</text></svg>">
    <style>
        :root {
            --primary: #2563eb;
            --secondary: #7c3aed;
            --success: #059669;
            --danger: #dc2626;
            --warning: #d97706;
            --light: #f8fafc;
            --dark: #1e293b;
        }
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
        }
        body {
            background: linear-gradient(135deg, #f1f5f9 0%, #e2e8f0 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .app-container {
            max-width: 100%;
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        .app-header {
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            color: white;
            padding: 25px 20px;
            text-align: center;
        }
        .app-title {
            font-size: 24px;
            font-weight: 700;
            margin-bottom: 5px;
        }
        .app-subtitle {
            opacity: 0.9;
            font-size: 14px;
        }
        .nav-tabs {
            display: flex;
            background: #f1f5f9;
            padding: 5px;
            overflow-x: auto;
        }
        .nav-tab {
            flex: 1;
            padding: 15px 10px;
            border: none;
            background: transparent;
            color: #64748b;
            font-weight: 600;
            font-size: 14px;
            cursor: pointer;
            white-space: nowrap;
            border-radius: 12px;
            transition: all 0.3s;
        }
        .nav-tab.active {
            background: white;
            color: var(--primary);
            box-shadow: 0 4px 12px rgba(37, 99, 235, 0.2);
        }
        .tab-content {
            display: none;
            padding: 20px;
            animation: fadeIn 0.3s;
        }
        .tab-content.active {
            display: block;
        }
        .form-card {
            background: #f8fafc;
            padding: 20px;
            border-radius: 15px;
            margin-bottom: 20px;
        }
        .input-group {
            margin-bottom: 15px;
        }
        label {
            display: block;
            margin-bottom: 8px;
            color: var(--dark);
            font-weight: 600;
            font-size: 14px;
        }
        input, select {
            width: 100%;
            padding: 14px;
            border: 2px solid #e2e8f0;
            border-radius: 10px;
            font-size: 16px;
            transition: border 0.3s;
        }
        input:focus, select:focus {
            outline: none;
            border-color: var(--primary);
        }
        .btn {
            padding: 16px 24px;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }
        .btn-primary {
            background: var(--primary);
            color: white;
            width: 100%;
        }
        .btn-primary:hover {
            background: #1d4ed8;
        }
        .btn-secondary {
            background: #64748b;
            color: white;
        }
        .btn-secondary:hover {
            background: #475569;
        }
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
            margin-top: 20px;
        }
        .stat-card {
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
            padding: 20px;
            border-radius: 15px;
            text-align: center;
            border: 2px solid #e2e8f0;
        }
        .stat-value {
            font-size: 32px;
            font-weight: 700;
            color: var(--primary);
            margin: 10px 0;
        }
        .planning-grid {
            overflow-x: auto;
            margin-top: 20px;
        }
        .planning-table {
            width: 100%;
            border-collapse: collapse;
            min-width: 600px;
        }
        .planning-table th {
            background: #f8fafc;
            padding: 15px 10px;
            text-align: center;
            font-weight: 600;
            color: var(--dark);
            border-bottom: 3px solid #e2e8f0;
            position: sticky;
            top: 0;
            z-index: 10;
        }
        .planning-table td {
            padding: 12px 8px;
            text-align: center;
            border: 1px solid #f1f5f9;
        }
        .planning-table tr:hover td {
            background: #f8fafc;
        }
        /* BADGES DE SHIFTS COMPLETS - CORRIGÉS */
        .shift-badge {
            padding: 6px 10px;
            border-radius: 8px;
            font-weight: 700;
            font-size: 12px;
            display: inline-block;
            min-width: 30px;
            color: white;
        }
        .shift-1 { background: #3b82f6; } /* Bleu - Matin */
        .shift-2 { background: #10b981; } /* Vert - Après-midi */
        .shift-3 { background: #8b5cf6; } /* Violet - Nuit */
        .shift-R { background: #64748b; } /* Gris - Repos */
        .shift-C { background: #f59e0b; } /* Orange - Congé */
        .shift-F { background: #d97706; } /* Orange foncé - Férié */
        .shift-M { background: #ef4444; } /* Rouge - Maladie */
        .shift-A { background: #8b5cf6; } /* Violet - Autre absence */
        .shift-- { 
            background: #f1f5f9; 
            color: #64748b; 
            border: 1px dashed #cbd5e1; 
        }
        
        .install-btn {
            position: fixed;
            bottom: 25px;
            right: 25px;
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            color: white;
            border: none;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            font-size: 24px;
            box-shadow: 0 8px 25px rgba(37, 99, 235, 0.4);
            z-index: 1000;
            animation: pulse 2s infinite;
            cursor: pointer;
        }
        
        /* NOUVEAUX STYLES POUR LES FORMULAIRES CONGÉS ET FÉRIÉS */
        .form-section {
            background: white;
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 20px;
            border: 2px solid #e2e8f0;
        }
        .form-section h4 {
            margin-top: 0;
            margin-bottom: 15px;
            color: var(--dark);
            font-size: 16px;
        }
        
        .btn-warning {
            background: #f59e0b;
            color: white;
        }
        .btn-warning:hover {
            background: #d97706;
        }
        .btn-success {
            background: #10b981;
            color: white;
        }
        .btn-success:hover {
            background: #059669;
        }
        
        /* LÉGENDE DES SHIFTS */
        .legend-grid {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-top: 20px;
            padding: 15px;
            background: #f8fafc;
            border-radius: 10px;
        }
        
        /* STYLES POUR LES JOURS FÉRIÉS */
        .ferie-fixe {
            background: #fef3c7 !important;
            color: #92400e !important;
            font-weight: bold;
        }
        
        .ferie-manuel {
            background: #fce7f3 !important;
            color: #be185d !important;
            font-weight: bold;
        }
        
        /* MODAL */
        .modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.5);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 1000;
        }
        
        .modal-content {
            background: white;
            padding: 20px;
            border-radius: 12px;
            max-width: 500px;
            margin: 20px;
            max-height: 80vh;
            overflow-y: auto;
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .nav-tab {
                padding: 12px 5px;
                font-size: 12px;
                min-width: 80px;
            }
            .stats-grid {
                grid-template-columns: 1fr;
            }
            .form-card {
                padding: 15px;
            }
            .planning-table {
                min-width: 800px;
            }
            body {
                padding: 10px;
            }
            .tab-content {
                padding: 15px;
            }
        }
        
        /* Pour les très petits écrans */
        @media (max-width: 480px) {
            .nav-tabs {
                flex-wrap: wrap;
            }
            .nav-tab {
                min-width: 70px;
                padding: 10px 5px;
                font-size: 11px;
            }
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
    </style>
</head>
<body>
    <div class="app-container">
        <div class="app-header">
            <h1 class="app-title">📅 PLANNING 2026</h1>
            <p class="app-subtitle">Gestion des agents de sécurité - PWA</p>
        </div>
        
        <nav class="nav-tabs">
            <button class="nav-tab active" data-tab="dashboard">Dashboard</button>
            <button class="nav-tab" data-tab="planning">Planning</button>
            <button class="nav-tab" data-tab="agents">Agents</button>
            <button class="nav-tab" data-tab="add">Ajouter</button>
            <button class="nav-tab" data-tab="conges">Congés</button>
            <button class="nav-tab" data-tab="feries">Jours Fériés</button>
            <button class="nav-tab" data-tab="stats">Statistiques</button>
        </nav>
        
        <!-- Dashboard Tab -->
        <div id="dashboard-tab" class="tab-content active">
            <div class="form-card">
                <h3 style="margin-bottom: 20px; color: var(--dark);">📊 Vue d'ensemble</h3>
                <div class="stats-grid">
                    <div class="stat-card">
                        <div>👥 Agents Actifs</div>
                        <div class="stat-value" id="total-agents">0</div>
                    </div>
                    <div class="stat-card">
                        <div>📅 Présents Aujourd'hui</div>
                        <div class="stat-value" id="present-today">0</div>
                    </div>
                    <div class="stat-card">
                        <div>🔄 Groupes</div>
                        <div class="stat-value" id="total-groupes">0</div>
                    </div>
                    <div class="stat-card">
                        <div>✅ En service</div>
                        <div class="stat-value" id="en-service">0</div>
                    </div>
                </div>
                
                <div id="groupes-stats" style="margin-top: 20px; text-align: center;">
                    <!-- Stats par groupe seront ajoutées ici -->
                </div>
            </div>
            
            <div class="form-card">
                <h3 style="margin-bottom: 15px; color: var(--dark);">📋 Actions rapides</h3>
                <button class="btn btn-primary" onclick="showTab('add')" style="margin-bottom: 10px;">
                    ➕ Ajouter un agent
                </button>
                <button class="btn btn-secondary" onclick="exportToExcel()">
                    📤 Exporter Excel
                </button>
            </div>
        </div>
        
        <!-- Planning Tab -->
        <div id="planning-tab" class="tab-content">
            <div class="form-card">
                <h3 style="margin-bottom: 15px; color: var(--dark);">📅 Planning Mensuel</h3>
                <div style="display: flex; gap: 10px; margin-bottom: 20px; flex-wrap: wrap;">
                    <div class="input-group" style="flex: 1; min-width: 150px;">
                        <label for="month-select">Mois</label>
                        <select id="month-select">
                            <option value="1">Janvier</option>
                            <option value="2">Février</option>
                            <option value="3">Mars</option>
                            <option value="4">Avril</option>
                            <option value="5">Mai</option>
                            <option value="6">Juin</option>
                            <option value="7">Juillet</option>
                            <option value="8">Août</option>
                            <option value="9">Septembre</option>
                            <option value="10">Octobre</option>
                            <option value="11">Novembre</option>
                            <option value="12">Décembre</option>
                        </select>
                    </div>
                    <div class="input-group" style="flex: 1; min-width: 150px;">
                        <label for="year-select">Année</label>
                        <select id="year-select">
                            <option value="2024">2024</option>
                            <option value="2025">2025</option>
                            <option value="2026" selected>2026</option>
                            <option value="2027">2027</option>
                        </select>
                    </div>
                    <div class="input-group" style="flex: 1; min-width: 150px;">
                        <label for="groupe-select">Groupe</label>
                        <select id="groupe-select">
                            <option value="all">Tous groupes</option>
                            <option value="A">Groupe A</option>
                            <option value="B">Groupe B</option>
                            <option value="C">Groupe C</option>
                            <option value="D">Groupe D</option>
                            <option value="E">Groupe E</option>
                        </select>
                    </div>
                </div>
                <div id="planning-result" class="planning-grid">
                    <!-- Planning sera généré ici -->
                </div>
            </div>
        </div>
        
        <!-- Agents Tab -->
        <div id="agents-tab" class="tab-content">
            <div class="form-card">
                <h3 style="margin-bottom: 20px; color: var(--dark);">👥 Liste des Agents</h3>
                <div id="agents-list">
                    <!-- Liste des agents -->
                </div>
            </div>
        </div>
        
        <!-- Add Agent Tab -->
        <div id="add-tab" class="tab-content">
            <div class="form-card">
                <h3 style="margin-bottom: 20px; color: var(--dark);">➕ Ajouter un Agent</h3>
                <form id="agent-form">
                    <div class="input-group">
                        <label for="code-agent">Code Agent *</label>
                        <input type="text" id="code-agent" placeholder="Ex: A01, B12, E05" required>
                    </div>
                    <div class="input-group">
                        <label for="nom-agent">Nom *</label>
                        <input type="text" id="nom-agent" placeholder="Nom de famille" required>
                    </div>
                    <div class="input-group">
                        <label for="prenom-agent">Prénom *</label>
                        <input type="text" id="prenom-agent" placeholder="Prénom" required>
                    </div>
                    <div class="input-group">
                        <label for="groupe-agent">Groupe *</label>
                        <select id="groupe-agent" required>
                            <option value="">Sélectionner un groupe</option>
                            <option value="A">Groupe A (Rotation 8j)</option>
                            <option value="B">Groupe B (Rotation 8j)</option>
                            <option value="C">Groupe C (Rotation 8j)</option>
                            <option value="D">Groupe D (Rotation 8j)</option>
                            <option value="E">Groupe E (5/7 Spécial)</option>
                        </select>
                    </div>
                    <div style="display: flex; gap: 10px; margin-top: 20px;">
                        <button type="submit" class="btn btn-primary" style="flex: 2;">
                            💾 Enregistrer l'agent
                        </button>
                        <button type="button" class="btn btn-secondary" onclick="showTab('agents')" style="flex: 1;">
                            Annuler
                        </button>
                    </div>
                </form>
            </div>
        </div>
        
        <!-- Congés Tab -->
        <div id="conges-tab" class="tab-content">
            <!-- Contenu généré dynamiquement par app.js -->
        </div>
        
        <!-- Jours Fériés Tab -->
        <div id="feries-tab" class="tab-content">
            <!-- Contenu généré dynamiquement par app.js -->
        </div>
        
        <!-- Statistiques Tab -->
        <div id="stats-tab" class="tab-content">
            <div class="form-card">
                <h3 style="margin-bottom: 20px; color: var(--dark);">📊 Statistiques Détaillées</h3>
                <div id="stats-result">
                    <!-- Statistiques -->
                </div>
            </div>
        </div>
    </div>
    
    <button id="install-button" class="install-btn" style="display: none;" onclick="installPWA()">📲</button>
    
    <!-- Scripts -->
    <script src="data.js"></script>
    <script src="app.js"></script>
    
    <script>
        // Gestion de l'installation PWA
        let deferredPrompt;
        
        window.addEventListener('beforeinstallprompt', (e) => {
            e.preventDefault();
            deferredPrompt = e;
            const installButton = document.getElementById('install-button');
            if (installButton) {
                installButton.style.display = 'block';
            }
        });
        
        function installPWA() {
            if (deferredPrompt) {
                deferredPrompt.prompt();
                deferredPrompt.userChoice.then((choiceResult) => {
                    if (choiceResult.outcome === 'accepted') {
                        console.log('PWA installé');
                    }
                    deferredPrompt = null;
                    const installButton = document.getElementById('install-button');
                    if (installButton) {
                        installButton.style.display = 'none';
                    }
                });
            }
        }
        
        window.addEventListener('appinstalled', () => {
            console.log('PWA installé avec succès');
            const installButton = document.getElementById('install-button');
            if (installButton) {
                installButton.style.display = 'none';
            }
        });
        
        // Vérifier si on est en PWA
        if (window.matchMedia('(display-mode: standalone)').matches || window.navigator.standalone) {
            const installButton = document.getElementById('install-button');
            if (installButton) {
                installButton.style.display = 'none';
            }
        }
    </script>
</body>
</html>
